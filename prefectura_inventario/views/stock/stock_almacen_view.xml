<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Modificar la vista de formulario para incluir el campo programa_id -->
        <record id="view_warehouse_inherited" model="ir.ui.view">
            <field name="name">stock.warehouse.inherited</field>
            <field name="model">stock.warehouse</field>
            <field name="inherit_id" ref="stock.view_warehouse"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='code']" position="after">
                    <field name="adm"/>
                    <field name="programa_id" options="{'no_create': True, 'no_open': True, 'no_create_edit': True}" readonly="not adm" force_save="1"/>
                </xpath>

            </field>
        </record>

      


        <record id="action_filter_warehouses_by_program" model="ir.actions.server">
            <field name="name">Ver Almacenes</field>
            <field name="model_id" ref="stock.model_stock_warehouse"/>
            <field name="binding_model_id" ref="stock.model_stock_warehouse"/>
            <field name="state">code</field>
            <field name="code">
                action = env['stock.warehouse'].filter_warehouses_by_program()
            </field>
            <field name="groups_id" eval="[(4, ref('stock.group_stock_user'))]"/>
        </record>

        <record id="stock.menu_action_warehouse_form" model="ir.ui.menu">
            <field name="action" ref="action_filter_warehouses_by_program"/>
        </record>


    </data>
</odoo>